import "../config"
import QtQuick

// Rótulo pequeno de seção (caller passa o texto já em maiúsculas).
Text {
    font.pixelSize: 11
    font.letterSpacing: 1.5
    font.bold: true
    color: Theme.textDim
}
